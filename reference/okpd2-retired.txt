# Коды ОКПД2, которых нет в действующем классификаторе.
#
# ПУСТО НЕ ПОТОМУ, ЧТО СНЯТЫХ КОДОВ НЕТ. Первый скрипт собирал список
# разностью нашего reference/okpd2.txt и свежей выгрузки классификатора — и
# нашёл ноль. Способ был неверный: наш файл шестизначный, а снятые коды в
# реестре стоят полные, и вычитать было нечего. Три снятых кода мы видели
# своими глазами на карточках: 20.15.31.000 (мочевина), 20.13.63.000
# (пероксид водорода), 20.14.61.000 (соединения с альдегидной группой).
#
# Настоящий список даёт scripts/okpd2-refresh.js: он сравнивает коды, которые
# РЕАЛЬНО СТОЯТ в записях реестра, со свежей выгрузкой классификатора.
#
#   node scripts/okpd2-refresh.js data/data-base/okpd.xlsx \
#     --retired reference/okpd2-retired.txt
#
# Пока файл в этом виде, ни один код снятым не помечается — окпд2Retired()
# просто всем отвечает «нет». Это не ошибка работы, а недоделанная правка.
#
# Собрано: не собрано, кодов: 0
